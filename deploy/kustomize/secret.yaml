# Template only — not applied by kustomization.yaml.
# Prefer: kubectl create secret generic qumulo-cosi-creds -n qumulo-cosi --from-literal=token='...'
# Do not commit real tokens.
apiVersion: v1
kind: Secret
metadata:
  name: qumulo-cosi-creds
  namespace: qumulo-cosi
type: Opaque
stringData:
  token: "REPLACE_ME"
  # username: "cosi-driver"
  # password: "REPLACE_ME"
